# Dependensi cepat tes + jalur REST (FastAPI Settings) bagi CI serta developer.
# Rekomendasikan pengembangan atas Python 3.11+; kombinasi di bawah memilih wheel bila tersedia.
# Untuk aplikasi GCP/Mongo lengkap pakai requirements.txt utama.

pytest>=7.4.4
pytest-cov>=4.1.0
langchain-core>=0.2.0
fastapi>=0.115.0
pydantic>=2.11.0
pydantic-settings>=2.6.0
structlog>=24.1.0
httpx>=0.27.0
